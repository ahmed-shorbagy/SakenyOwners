import 'package:flutter/material.dart';

void main() {
  runApp(const SakenyOwners());
}

class SakenyOwners extends StatelessWidget {
  const SakenyOwners({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router();
  }
}
